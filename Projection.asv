function [P_projected] = Projection(P1,P)
%UNTITLED2 Summary of this function goes here
%   Detailed explanation goes here


P1=[P1(:,1)';P1(:,2)'; ones(1,5);ones(1,5)];
P_projected= P*P1;

imshow(frame); % creates a new window for each image
hold on

for i=1:5
hold on
plot([P_projected(1,i),P_new(1,i)],[P_projected(2,i),P_new(2,i)],'y-',LineWidth=3)
end
 plot(P_projected(:,1), P_projected(:,2),'b',LineWidth=2)
 hold on
 plot(P_new(:,1),P_new(:,2),'r',LineWidth=2);
 hold on
 pause(0.1);
end